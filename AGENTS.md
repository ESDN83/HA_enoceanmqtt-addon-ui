# AGENTS.md

This project keeps its AI and developer context in [CLAUDE.md](CLAUDE.md), with detail in [docs/](docs/). Read those first.

`AGENTS.md` exists so tools that look for this filename find the same single source of truth. Do not duplicate content here; update `CLAUDE.md` and `docs/` instead.
